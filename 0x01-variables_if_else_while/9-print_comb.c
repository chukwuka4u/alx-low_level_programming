#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int num = '0';

	while (num <= '9')
	{
		putchar(num);
		if (d != '9')
		{
			putchar(',');
			putchar(' ');
		}
	}
	puchar('\n');

	return (0);
}
